# Scripts

Anything I've automated during this journey. Each script lives in its own folder with its own README explaining what it does.

## Rules
- No live credentials or API keys (use environment variables, see `.gitignore`)
- Each script has a README + usage example
- Comment generously — these are learning artifacts

## Index
_Add scripts as you write them._
